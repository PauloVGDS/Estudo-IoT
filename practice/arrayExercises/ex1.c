#include <stdio.h>
#include <stdlib.h>

int main() {
    int numeros[5] = {5, 13, 17, 23, 50}; // TODO: inicialize com valores de sua escolha
    // TODO: preencha e imprima os valores
    for(int i = 0; i < 5; i++) {
        printf("%d\n", numeros[i]);
    }
    return 0;
}

